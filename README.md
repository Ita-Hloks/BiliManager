# BiliManager

BiliManager 是一个 Manifest V3 浏览器扩展，使用 React、TypeScript、Tailwind 和 Vite 开发。

```bash
npm install
npm run typecheck
npm run build
```

## 加载扩展

推荐在 Chrome / Edge 的扩展管理页加载：

`dist/` 是 `npm run build` 生成的扩展产物

## 功能介绍

- 没有！
- 什么都没有！
- 完全就是个空壳子！

## Git 提交建议

- 也没有！
